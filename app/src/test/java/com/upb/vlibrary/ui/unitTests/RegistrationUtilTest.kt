package com.upb.vlibrary.ui.unitTests

import com.google.common.truth.Truth.assertThat
import com.upb.vlibrary.ui.register.RegisterFragment
import org.junit.Test

class RegistrationUtilTest {

/*
    @Test
    fun `empty username returns false`() {
        val result = RegisterFragment().validateRegistrationInput(
            RegisterFragment().binding.userName.text.toString(),
            RegisterFragment().binding.userEmail.text.toString(),
            RegisterFragment().binding.userPassword.text.toString(),
        )
        assertThat(result).isFalse()
    }

    @Test
    fun `valid username and correctly repeated password returns true`() {
        val result = RegisterFragment().validateRegistrationInput(
            "Philipp",
            "123",
            "123"
        )
        assertThat(result).isTrue()
    }

    @Test
    fun `username already exists returns false`() {
        val result = RegisterFragment().validateRegistrationInput(
            "Carl",
            "123",
            "123"
        )
        assertThat(result).isFalse()
    }

    @Test
    fun `incorrectly confirmed password returns false`() {
        val result = RegisterFragment().validateRegistrationInput(
            "Philipp",
            "123456",
            "abcdefg"
        )
        assertThat(result).isFalse()
    }

    @Test
    fun `empty password returns false`() {
        val result = RegisterFragment().validateRegistrationInput(
            "Philipp",
            "",
            ""
        )
        assertThat(result).isFalse()
    }

    @Test
    fun `less than 2 digit password returns false`() {
        val result = RegisterFragment().validateRegistrationInput(
            "Philipp",
            "abcdefg5",
            "abcdefg5"
        )
        assertThat(result).isFalse()
    } */
}